export const userSchema = {
  type: 'object',
  properties: {
    id: { type: 'number' },
    data: {
      type: 'object',
      properties: {
        name: { type: 'string' },
      },
      required: ['name'],
    },
  },
  required: ['id', 'data'],
  additionalProperties: true,
} as const;
